#!/usr/bin/env sh

if gem which foreman > /dev/null 2>&1; then
  exec foreman start -f Procfile.dev "$@"
else
  echo "Installing foreman..."
  gem install foreman
  exec foreman start -f Procfile.dev "$@"
fi
